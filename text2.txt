/* General container styling */
.login-container {
    display: flex;
    height: 100vh;
    font-family: Arial, sans-serif;
  }
  
  /* Left Section */
  .login-left {
    flex: 1;
    padding: 2rem;
    background-color: whitesmoke;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    min-width: 300px;
    box-sizing: border-box;
  }
  
  /* Right Section (Slider) */
  .login-right {
    flex: 2;
    position: relative;
    overflow: hidden;
    height: 100%; /* Make sure the slider takes the full height */
  }
  /* Style for the login button */
.login-button {
    background-color: white;  /* Set background color to white */
    color: #333;              /* Set text color to a dark color (you can adjust as needed) */
    border: 1px solid #ccc;   /* Add a light border for the button */
    padding: 10px 20px;       /* Adjust padding for a nice button size */
    font-size: 1rem;          /* Adjust font size */
    cursor: pointer;         /* Show pointer on hover */
    transition: background-color 0.3s ease;  /* Add a transition effect on hover */
  }
  
  /* Style for Google login button */
  .google-login {
    background-color: white;  /* Set background color to white */
    color: #333;              /* Set text color to a dark color */
    border: 1px solid #ccc;   /* Add a light border for the button */
    padding: 10px 20px;       /* Adjust padding for the button size */
    font-size: 1rem;          /* Adjust font size */
    display: flex;            /* Flexbox to align text and image */
    align-items: center;      /* Center align items vertically */
    cursor: pointer;         /* Show pointer on hover */
    transition: background-color 0.3s ease;  /* Add a transition effect on hover */
  }
  
  /* Optional: If you want to change the background on hover */
  .login-button:hover, 
  .google-login:hover {
    background-color: #f0f0f0; /* Light gray background on hover */
  }
  
  /* Optional: Styling the Google logo inside the button */
  .google-logo {
    margin-right: 10px;  /* Space between logo and text */
    height: 20px;        /* Set the logo height */
    width: auto;         /* Maintain aspect ratio */
  }
  
  
  .slider {
    position: relative;
    height: 100%;
    display: flex;
    transition: transform 1s ease-in-out;
    width: 100%; /* Ensure slider takes the full width of the parent */
  }
  
  /* Each slide item */
  .slider-item {
    min-width: 100%;
    height: 100%;
    position: relative;
    opacity: 0;
    transform: translateX(100%);
    transition: all 1s ease-in-out;
  }
  
  /* Active slide */
  .slider-item.active {
    opacity: 1;
    transform: translateX(0%);
  }
  
  /* Slider image styling */
  .slider-image {
    width: 100%;
    height: 100%;
    object-fit: cover;  /* Ensures the image covers the entire area */
    object-position: center; /* Ensures the image is centered within the container */
    transition: filter 1s ease-in-out;
  }
  
  /* Blur effect for next image */
  .slider-image.next {
    filter: blur(4px);
  }
  
  /* Text overlay on the slider */
  .slider-text {
    position: absolute;
    top: 10%;
    left: 50%;
    transform: translateX(-50%);
    text-align: center;
    color: white;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
    z-index: 2;
  }
  
  .slider-text h1 {
    font-size: 2rem;
    margin: 0;
  }
  
  .slider-text p {
    font-size: 1rem;
    margin: 10px 0 0;
  }
  